{
  "version": 2,
  "builds": [
    {
      "src": "adn-checker/public/index.php",
      "use": "@vercel/php"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "adn-checker/public/index.php"
    }
  ],
  "installCommand": "cd adn-checker && curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --optimize-autoloader && php artisan key:generate"
}
